# Phoenix Trading System v3.1 (CUSTOM) - 빌드 전용 의존성
# PyInstaller EXE 빌드 시 사용

# 기본 의존성 (requirements.txt 포함)
-r requirements.txt

# PyInstaller (EXE 빌드)
pyinstaller==6.3.0

# 추가 빌드 도구
setuptools>=65.0.0
wheel>=0.42.0

# ====== 빌드 방법 ======
#
# 1. 32비트 Python 환경 확인
#    python -c "import struct; print(struct.calcsize('P') * 8)"
#    출력: 32 (필수!)
#
# 2. 빌드 의존성 설치
#    pip install -r requirements_build.txt
#
# 3. EXE 빌드 실행
#    방법 1: build_exe.bat 실행 (권장)
#    방법 2: pyinstaller phoenix.spec --clean
#
# 4. 빌드 결과 확인
#    dist\PhoenixTrading\PhoenixTrading.exe
#
# ====== 주의사항 ======
#
# - ⚠️ 반드시 32비트 Python에서 빌드하세요
# - 64비트 Python에서는 키움 OpenAPI가 작동하지 않습니다
# - 가상환경 사용을 권장합니다
# - 빌드 시간: 약 3~5분 소요
# - 빌드 결과 파일 크기: 약 50~80MB
